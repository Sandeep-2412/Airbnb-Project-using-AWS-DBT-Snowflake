[project]
name = "aws-dbt-snowflake"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "dbt>=1.0.0.40.14",
    "dbt-core>=1.11.2",
    "dbt-snowflake>=1.11.1",
]


